# Security Examples

This folder contains intentionally vulnerable or comparison code used for security education.

Do not run these examples in production. The supported model download service is `model_server.py` in the repository root.

The vulnerable example is retained only to explain why authentication, file type checks, and path traversal protection matter.
